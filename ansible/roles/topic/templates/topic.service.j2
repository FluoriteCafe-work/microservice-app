[Unit]
Description=Topic microservice
After=network.target

[Service]
Type=simple
ExecStart={{ install_dir }}/topic \
  -addr={{ ansible_host }}:8084 \
  -etcd.addr={{ etcd_endpoint }}
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
